import { ReactNode } from "react";
interface CardProps {
    className?: string;
    title: string;
    children: ReactNode;
    href: string;
}
export declare const Card: ({ className, title, children, href }: CardProps) => import("react").JSX.Element;
export {};
//# sourceMappingURL=card.d.ts.map